import React, {useMemo} from 'react'


import Lotus from './Component';
import d3 from './d3';

// TODO use d3 through hooks to combine view and logic
const LotusD3 = ({dimension}) => {
    // const memoised = useMemo(() => d3(), [1]);
    return (
        < Lotus />
    )
    }

export default LotusD3;
